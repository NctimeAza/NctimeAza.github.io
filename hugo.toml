baseURL = '/'
languageCode = 'zh-cn'
defaultContentLanguage = 'zh-cn'
title = 'My Blog'
theme = 'stack'
pagination.pagerSize = 5

[permalinks]
    post = '/post/:slug/'
    page = '/:slug/'

[params]
    mainSections = ['post']
    favicon = 'img/avatar.png'

    [params.footer]
        since = 2026
        customText = ''

    [params.dateFormat]
        published = 'Jan 02, 2006'
        lastUpdated = 'Jan 02, 2006'

    [params.sidebar]
        subtitle = 'Abstractness is the price of generality'
        emoji = ''
        compact = false
        avatar = 'img/avatar.png'

    [params.article]
        headingAnchor = true
        math = false
        toc = true
        readingTime = true

        [params.article.list]
            showTags = true

        [params.article.license]
            enabled = true
            default = 'Licensed under CC BY-NC-SA 4.0'

    [params.widgets]
        homepage = [
            { type = 'search' },
            { type = 'archives', params = { limit = 5 } },
            { type = 'tag-cloud', params = { limit = 10 } }
        ]
        page = [
            { type = 'toc' },
            { type = 'search' }
        ]

    [params.opengraph.twitter]
        site = ''
        card = 'summary_large_image'

    [params.colorScheme]
        toggle = true
        default = 'auto'

    [params.comments]
        enabled = false

# 主菜单在各页面 front matter 中定义(menu.main),此处勿重复定义,否则菜单项会重复
# 示例见 content/_index.md、content/page/about/index.md 等

[[menu.social]]
    identifier = 'github'
    name = 'GitHub'
    url = 'https://github.com/your-username'
    pre = 'github'
